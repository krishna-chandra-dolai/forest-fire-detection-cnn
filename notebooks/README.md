# Notebooks Folder

Use this folder for Jupyter Notebook experiments, model training runs, visualizations, and evaluation reports.

Included notebooks:

- `forest_fire_detection_cnn_resnet50.ipynb`: CNN/ResNet-50 notebook created from the project report and source scaffold.
- `legacy_forest_fire_area_prediction.ipynb`: original notebook found inside the local `Predict-the-Forest-Fires.7z` archive. This is a CSV regression project, not the CNN image classifier.
